# Integrated Learning Autoflow v1.2 SSOT

This directory is the SHA-fixed verification target for Phase 1.

It intentionally contains only digest/ref fixtures. It does not contain raw
transaction text, file names, prompts, responses, local paths, account numbers,
amounts, or customer names.

Group A should measure only SHA-fixed artifacts, never rotating three-sample
extracts, when judging L9a/L9b accuracy and regression gates.
