SECURE_UPDATE_TUF_PRINCIPLES_SSOT_V1_TOKEN=1

# repo layout (metadata root)
TUF_META_ROOT=out/ops/tuf

# required roles (minimal TUF set)
REQUIRE_ROLE_ROOT=1
REQUIRE_ROLE_TARGETS=1
REQUIRE_ROLE_SNAPSHOT=1
REQUIRE_ROLE_TIMESTAMP=1

# expiry policy (days)
ROOT_EXPIRES_DAYS_MAX=365
TARGETS_EXPIRES_DAYS_MAX=90
SNAPSHOT_EXPIRES_DAYS_MAX=30
TIMESTAMP_EXPIRES_DAYS_MAX=7

# threshold policy (minimal)
ROOT_THRESHOLD_MIN=1
TARGETS_THRESHOLD_MIN=1
SNAPSHOT_THRESHOLD_MIN=1
TIMESTAMP_THRESHOLD_MIN=1

# forbidden patterns (meta-only checks)
FORBID_UNVERSIONED_TARGETS=1
FORBID_UNSIGNED_METADATA=1
