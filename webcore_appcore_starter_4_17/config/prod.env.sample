# BFF Accounting - Production Environment Variables
# 운영 환경에서는 실제 값으로 교체하세요

# 포트
PORT=8081

# PostgreSQL 영구화 사용
USE_PG=1

# PostgreSQL 연결 문자열
DATABASE_URL=postgres://app:***@db.prod:5432/app

# Export 서명 시크릿 (변경/주기적 로테이션 필수)
EXPORT_SIGN_SECRET=*** # 변경/주기적 로테이션 필수


