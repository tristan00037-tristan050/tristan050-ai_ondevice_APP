apiVersion: v1
kind: Service
metadata:
  name: {{ include "bff-accounting.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector: { app: {{ include "bff-accounting.name" . }} }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8081

